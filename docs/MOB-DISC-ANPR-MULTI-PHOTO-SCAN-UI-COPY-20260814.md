# MOB-DISC ANPR multi-photo scan UI copy + layout — 2026-08-14

**Status:** applied with this pass (operator ordered layout/copy fix).  
**Rule:** `me8-ui-copy-professional` — no teach strips on a clear control.

## Locked UX

| Do | Don’t |
|----|--------|
| Bigger drop zone (left) | “Batch scan” title |
| Results on the **right** with **Results** heading only when rows appear | Hint / essay (“reads plates and checks…”) |
| Drop label: `Drop photos here (max 100)` | “Batch selected”, “Run batch scan”, “Clear batch” |
| Actions: **Scan** / **Clear** / Hits only | Permanent coach copy under the control |
| Count in zone: `3 / 100` | “N photo(s) selected…” |

Snapshot single card unchanged. Max 100 unchanged.
