# MOB DISC — Log proof: what you are playing is **not** ZLM · you want WVP/ZLM **now**

**Status:** LOCKED 2026-07-16 ~22:14  
**Search:** `is it zlm`, `100% sure`, `want now`, `check log`, `competent`  
**Proof rule:** only `live broker wvp-zlm primary` = WVP-ZLM. Invite + FFmpeg ≠ ZLM.

---

## Log check (your play session)

While you were on the **normal panel** (~22:11–22:13):

| Signal | Count / fact |
|--------|----------------|
| `live broker wvp-zlm primary` | **0** |
| Fleet SIP invite accepted | **yes** (chin / kk) |
| `pool ffmpeg` | **yes** (Fleet path) |
| Wall soft ZLM | **off** (parked on purpose after bad APPLY) |

**Verdict: you are 100% correct — this video is Fleet / FFmpeg, not WVP-ZLM.**

---

## What **you** do right now (baby)

1. Keep using the **normal panel** if you need picture for work.  
2. Do **not** open Lab two-tiles.  
3. Do **not** change camera SIP (stay **5060**).  
4. To make me work ZLM **now**, say one line below (APPLY). That is the trigger — not waiting in silence.

---

## Why ZLM is not on the panel yet (honest, short)

| Fact | Meaning |
|------|---------|
| Soft ZLM on wall | Turned **off** after it broke live |
| Stack cleanup | Server plumbing only — does **not** switch your player |
| WVP sees cams online | **0** last check — WVP play cannot succeed until that changes (agent/lab problem; **not** “you rewrite 5060”) |

So: wanting ZLM now is fair. **Refresh cannot invent it.** Next step is a **named MOB** I apply, then you look at the picture.

---

## What I need from you to start **now**

Say exactly one:

**`MOB-APPLY mob-wvp-zlm-soft-after-ffmpeg-failopen-v1`**

That means (plain):

- Keep Fleet picture first (no invite hold — no black panel from last FAIL)  
- After picture is up, try WVP-ZLM in the background  
- If ZLM works → switch/overlay; if not → **stay on Fleet** (fail-open)  
- You still only: normal panel → pass/fail from picture  
- I check for `live broker wvp-zlm primary`  

**I will not start that code until you say that APPLY.**

If soft-after still cannot get ZLM (WVP still no online cam), I say so in one plain sentence — no lab page, no 5061 homework for you.

---

## One line

**Log: not ZLM (Fleet/FFmpeg). You keep the normal panel. Want ZLM now → say `MOB-APPLY mob-wvp-zlm-soft-after-ffmpeg-failopen-v1`.**
