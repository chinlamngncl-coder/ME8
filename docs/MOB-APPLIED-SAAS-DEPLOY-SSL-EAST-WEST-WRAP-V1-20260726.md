# MOB-APPLIED — SAAS-DEPLOY-SSL-EAST-WEST-WRAP-V1

**Date:** 2026-07-26  
**APPLY:** `SAAS-DEPLOY-SSL-EAST-WEST-WRAP-V1`  
**Scope:** HTML only in `public/index.html` — no JS, no new CSS

## Done

- `#ss-section-deployment`: fields wrapped in `.ss-east-west-grid`; IDs/options unchanged; `#ss-deployment-hint` stays `.setup-hint` below grid
- `#ss-section-ssl`: cert/key wrapped in `.ss-east-west-grid` (replaced `.ss-ssl-form`); `#ss-ssl-status` below; IDs unchanged

## Operator

Ctrl+F5 → Settings → Server Config → Network & deployment → Deployment + SSL side-by-side on wide screens.
