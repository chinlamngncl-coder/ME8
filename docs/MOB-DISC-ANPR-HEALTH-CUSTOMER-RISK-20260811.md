# MOB DISC — Customer next time? ANPR health 500 — 2026-08-11

**Status:** PAPER (with APPLY already done for fix).

| Question | Answer |
|----------|--------|
| Will customers hit this? | **Yes, if we shipped the SyntaxError.** Same badge forever. |
| Is Start watch the engine? | **No.** Sidecar must run + `/health` must be 2xx/`ok`. |
| Fixed for ship? | **This APPLY** — restart sidecar to load fix; gate pack until health OK. |

Do not pack until operator PASS: Engine OK + one capture.
