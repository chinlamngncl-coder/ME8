# MOB DISC — What to APPLY next (pin cleanup — one name)

**Date:** 2026-07-21 ~01:15 · updated ~01:16  
**Status:** LOCKED — **one APPLY name** · no code until you type it  
**Operator:** *Tell me what to apply then?*  
**Also locked:** *Call is still not working. Remember. It is also in baseline. We don’t have to redo.*

---

## Answer — type exactly this

```text
MOB-APPLY PIN-FLEET-BASELINE-PLAYER-ONLY-CLEANUP-V1
```

That is the **cleanup back to the one rule** — not a new invention.

---

## Call (locked — do not reinvent)

| Fact | Rule |
|------|------|
| Call still FAIL on desk | **Noted** — part of this cleanup genre |
| Classic / Gold | Call already designed (`toggleVoiceCall` / `start-bwc-call` / pin+wall Call UI in baseline `video-wall.js`) |
| Cleanup must | Diff/restore baseline **Call** with pin+wall live — if scarred by WVP/pin MOBs, **bring classic back** |
| Cleanup must not | Redesign Call, new voice stack, or a separate “redo Call” MOB |

**Same as pin:** Fleet baseline has Call. Don’t invent. Restore.

---

## What that APPLY means (locked intent)

| Do | Do not |
|----|--------|
| Diff live pin path vs `baseline/2026-07-18-classic-pass` (Gold mirror contract) | Invent FORCE-START / NO-TOPLEFT / FOCUSED-OPEN |
| Remove post-graft scars that replaced classic play/attach | New dock product |
| Keep only WVP paint: wall FLV + mirror from `<video.me8-zlm-primary>` | Second FLV; turn handoff off |
| **Preserve/restore baseline Call** (`start-bwc-call` / pin Call chrome) | Rewrite Call from scratch |
| Cache-bust `video-wall.js` | Tactical / vuln / Evidence |

**One rule:** Fleet baseline pin UX **+ Call** + WVP paint only.

---

## Operator check after APPLY

1. `RESTART-FLEET.bat` → Run as administrator → hard refresh.  
2. Pin-only click → picture.  
3. **Call** on that pin (baseline).  

| PASS | FAIL |
|------|------|
| Picture + **Call works** | Say: video / Call / jump |

---

## Bottom line

**Apply:** `MOB-APPLY PIN-FLEET-BASELINE-PLAYER-ONLY-CLEANUP-V1`  
**Remember:** Call FAIL = baseline restore, **not redo.**
