# MOB DISC — Case desk includes Redact (Evidence + Docking already) (2026-08-09)

**Status:** LOCKED. Operator: “and redact if necessary — that is why redact is in Evidence and docking. Got it?”  
**Yes.** Checked EMS (Axon): case evidence actions include **Redact**. ME8 already placed Redact under **Evidence** and **Docking** — that is intentional, not optional chrome.

---

## Lock

| Piece | Rule |
|-------|------|
| Open case (target) | Video + inputs + map pin **+ redact when needed** |
| Where Redact lives today | **Evidence** and **Docking** (operator already put it there) |
| Why | Same reason as Axon: before share/export/settle, black out / mute sensitive bits; **original stays**; redacted copy for disclosure |
| Not a separate product | Redact is part of the **evidence case workflow**, not a side toy |

---

## With prior EMS disc

`MOB-DISC-EMS-CASE-OPEN-SEES-VIDEO-MAP-NOTES-CHECKED-20260809.md`  
Open case → play clips → notes/fields → map → **redact if necessary** → then close/settle.

When building `OPS-CASE-OPEN-DESK-V1` / bind evidence: **reuse existing Evidence/Docking redact**, do not invent a second redact stack.

---

## `.cursorrules`

UI form/grid + CREDIT-LEAN-HARD. Product EMS locks live in MOB DISCs (this file + EMS case-open disc).
