# MOB DISC — Redaction tab: honest status (not “enterprise done”)

**Status:** LOCKED 2026-07-17 ~00:28  
**Search:** `cheated`, `redaction tab done`, `what is running`, `do not wanna try`  
**Trigger:** Operator screenshot — Controlled preview + custody “face-follow” + redacted files listed  
**Genre:** Evidence only (not WVP / wall / FR live)

---

## Straight answer

| Claim | Truth |
|-------|--------|
| “Redaction tab is done” = **enterprise / ship PASS** | **NO. That was overclaim if said that way.** |
| Some named MOBs **APPLIED** (code landed) | **YES** — code exists; **desk ship-gate PASS still open** |
| UI looks finished → quality PASS | **NO** — UI chrome ≠ blur quality / save UX PASS |
| Fake / empty tab with no backend | **NO** — not a dummy shell; real save path + audit + files |

**Not cheated as “fake UI with no files.”**  
**Did cheat if agent said “redact done / ship ready” without your desk PASS.** That was wrong.

---

## What your screenshot actually shows (running now)

| What you see | What it means |
|--------------|----------------|
| **Controlled preview** / “hidden by default” / Open Preview | Privacy gate on detail preview — **real UI**, not redact burn quality |
| Custody: “Video redaction opened” / “Redacted copy saved” · Mode: **face-follow** | Audit for in-app redact path that was APPLIED |
| `[Redacted] …_redacted_….mp4` · Finalized Download | Redacted **copies** were written + finalize path used |
| Redact video / Download chips | Detail actions (compact-actions MOB) |

So: **the tab is wired and has produced files.** That is not proof faces are redacted well, or that Auto/Save UX is client-ready.

---

## What is actually running (engine)

| Piece | Reality |
|-------|---------|
| Entry | Evidence Hub → **Redact video** → in-app workspace |
| Auto / burn | Face-follow burn when Save with `faceFollow` — detector may be **Seeta** (default after seeta-detect MOB) or **YuNet** if `FM_REDACT_FACE_ENGINE=yunet` |
| Output | New `*_redacted*.mp4` — **original sealed** (by design) |
| Audit | `evidence.redact_start` / `evidence.redact_save` / finalize labels in custody trail |

Detector switch ≠ quality PASS. Auto once “blurred everything” on YuNet static path; face-follow code landed — **you never gave desk PASS.**

---

## What APPLIED vs what is NOT PASS

### Code MOBs that landed (examples)

- `mob-evidence-redact-face-follow-v1` — per-frame face-follow burn  
- `mob-evidence-redact-auto-preview-slim-v1` — fewer Auto preview rows  
- Detail compact actions / controlled preview (evidence UX)

### Explicitly **not** ship / enterprise done

| Gate | Status |
|------|--------|
| Desk ship-gate: one clip, faces follow, you say PASS | **Still required** (`MOB-DISC-REDACT-FACE-FOLLOW.md`) |
| Auto quality (“blur everything” FAIL) | **Not cleared** — quality MOB papered, not closed |
| Save hang / details UX / Redact buried | **Paper discs — not all APPLIED** |
| “I don’t even wanna try” | Valid — agent must **not** call tab done |

---

## Forbidden agent language (from now)

- “Redaction tab is done”  
- “Enterprise redact PASS”  
- “Ship-ready redact”  

**Allowed:** “Named MOB APPLIED — code in; **your desk PASS not given**.”

---

## What to do next (only when you want)

1. Stay on WVP/ZLM until that genre is stable (your call).  
2. Later: one honest desk clip — Open Preview / Auto / Save — you say PASS or FAIL.  
3. Named follow-ups only with **MOB-APPLY** (quality / save progress / action top — already papered).

No code in this disc.

---

## One line

**Tab is real and has saved face-follow copies — that is not “redact done.” Ship/desk PASS still open; agent must not overclaim.**
