# MOB DISC — Consolidated audit fix plan (2026-09-02)

**Sources reconciled**
- `Desktop\axiom_logic_audit.md` — audit run 1 (video/timeline state, alarm schema, offline fallbacks, SOS/DB races)
- `Desktop\axiom_logic_audit 1.md` — audit run 2 (PTT routing, tab teardown, sidecar downloads, fetch timeouts, ledger temp clobber, SIP tag, DB locks)
- Google synthesis (5-MOB backlog) — correct priorities, incomplete coverage

**Status:** paper only. No code changed by this disc. Every item below is one `MOB-APPLY` → restart / refresh → operator PASS → checkpoint → next.

**Restore floor for this campaign:** `me8-firmware-sep-gold-20260902` (`RUN RESTORE-ME8-FIRMWARE-SEP-GOLD`). Older `me8-firmware-gold-20260706` stays as deep fallback.

---

## Reconciliation

- Reports do not contradict; they complete each other. Google's list dropped ~30 High/Med rows and the report-1 Critical compare-layout closure bug.
- Gesture fix is split precisely: real defects are unhandled `play()` promises after fetch and unmuted play in FR dialogs. `tlPlayVideoElement` and `AxiomFlvManager.attach` already do muted-first and are **not** rewritten.
- Locked files shape the plan: `lib/pttServer.js`, `public/js/video-wall.js`, pin-sync functions in `public/index.html` are do-not-touch unless the user names the file. PTT fix lives in `server.js` socket handlers + `lib/pttFieldGroupRelay.js`. `lib/deviceControl.js` `udp_once` untouched. `lib/sipServer.js` does not exist.
- Order principle: server data/audio safety → ship blockers (air gap) → browser stability → schema → DB → hygiene.

---

## Phase 1 — Audio safety and SOS data integrity (server, restart required)

| # | MOB | Files / lines | Change | Crash / regression guard | Operator PASS |
|---|---|---|---|---|---|
| 1 | `PTT-HQ-SINGLE-FLOOR-V1` — **APPLIED 2026-09-02** | `lib/pttFieldGroupRelay.js` (`beginHqFloor` returns `{ok}` / `{ok:false,busy,holderSocketId,overlap}`, new `touchHqFloor`, stale reclaim `max(floorStaleMs, 5 s)` since last frame, counter `hqFloorBusy`); `server.js` `ptt-start` (acquire before arming targets; busy → talk-state error "Another operator is talking to this BWC — wait for release", no targets set), `ptt-audio` (no targets → drop; `touchHqFloor` keeps holder alive) | Per-target HQ floor: second operator whose targets overlap a live holder is refused; frames from a refused socket are dropped (old `meta.camId` fallback removed — client only sends `ptt-audio` after `ptt-start` and already stops mic on talk-state error). Release on `ptt-stop`, `disconnect`, stale. Disjoint targets (A→cam8, B→cam7) both allowed. | `pttServer.js` untouched. No client edit. Lab test: A(8,9) ok → B(9) busy → C(7) ok → A stop → B ok; stale reclaim after 5 s silence. | Operator: two HQ tabs, same BWC, PTT on both → first talks, second gets busy alert; release first → second talks. Single-tab PTT unchanged. |
| 2 | `PTT-PER-SOCKET-VOICE-TARGET-V1` — **APPLIED 2026-09-02** | `server.js`: new `pttVoiceCallOwnerSocketId` beside `pttVoiceCallCamId`; set in `launchOutboundTalkCall` + `launchFleetVoiceBroadcast` (= caller socket), `null` on late-inbound-INVITE path and on owner `disconnect`; `call-audio` drops frames from any non-owner socket (rate-limited warn `call audio dropped — not call owner`); `ptt-audio` half already done by MOB 1 | Voice call is server-wide single-call by design (`mediaSession.getVoiceCallCamId()` is singular) — so the real cross-talk was a *second tab's* `call-audio` landing in the first tab's call. Owner-null (legacy/inbound/after owner reload) keeps the old camId-match rule, so nothing existing is narrowed. Client always sends `meta.camId` (`call-mic.js`). | Single-operator call unchanged. Syntax OK. | Operator: A calls BWC, B tab presses call-mic → B audio not heard on BWC, log shows drop; A reload mid-call → A can still end the call. |
| 3 | `SOS-LEDGER-WRITER-QUEUE-V1` — **APPLIED 2026-09-02 (write-behind form)** | `lib/sosIncidents.js` header, 24-110, `recordAlarm` id, ack id, `init` | Risk-assessment redesign: in-memory store is source of truth (all mutators stay **sync**, same return values); disk flush is write-behind, coalesced per file, one sync write per tick (no interleaving possible), unique temp `<file>.<pid>.<rand>.tmp` + rename; failed flush logged, never poisons others; `process.on('exit')` flushes pending. Ids `alarm-<ms>-<6hex>` / `ack-<ms>-<6hex>` (prefix kept). `init()` resets caches. | Callers unchanged. `server.js` report path that reads `ledger.json` from disk lags at most one tick. Lab test: 3 raises same tick → 3 unique ids, sync `hasOpenAlarm` true, disk 3 entries after tick, 0 `.tmp`, clear works. | Operator: restart Fleet → cold SOS → ack → −helper; ledger shows the incident; `active-alarm.json` clears on ack as before. |
| 4 | `SOS-RAISE-ATOMIC-AND-PER-CAM-ACTIVE-V1` — **APPLIED 2026-09-02 (narrowed on read)** | `lib/sosIncidents.js` only: `active-alarm.json` → `{ byCam: { <camId>: entry } }` (legacy single-entry file migrated on first read); `readActiveAlarm(camId?)` (no arg = most recent, old contract), `readActiveAlarms()`, `writeActiveAlarm` per cam, `clearActiveAlarm(camId?)`; `recordAcknowledgement` clears only the acked cam; `getOpenAlarms` / `hasOpenAlarm` / `attachSnapshotFromFtp` read per cam | Verified on read, **no code needed**: (a) `raiseDeviceAlarm` is fully sync and since MOB 3 `hasOpenAlarm→recordAlarm` is in-memory sync → open-or-merge already atomic; (b) `sendDeviceControl` is sync → check→send→arm already atomic; (c) `scheduleSosCapture` is a no-op (dead timer list). Real bug fixed: cam B's SOS overwrote cam A's active entry, and acking either cam cleared active for both. `deviceAlarm.js`, `server.js`, `deviceControl.js` untouched. | Lab test: legacy file migrates; A+B raise → both open, latest=B; ack A → A closed, B and legacy still open; disk `byCam` = {B,L}; file removed when empty; 0 `.tmp`. | Operator: SOS on 2 cams → both banners; ack cam 1 → cam 2 stays in alarm; ack cam 2 → clear. |
| 5 | `SOS-INVITE-GUARDS-V1` — **APPLIED 2026-09-02** | `server.js` `startVideoForSosAlarm` entry: `sosInviteLock.tryAcquire(camId, 13 s)` (covers the 12 s contact-retry window; early release paths unchanged: streaming / no_contact / invite_failed / queue `onStreamStarted`), refused → `sos server pull skipped reason:sos_connect_lock`; `lib/liveStreamPool.js` `sendSessionInvite` 200 branch: late 200 whose Call-ID ≠ the live `activeDialog` is ACKed + BYEd and logged `pool stray 200 ignored` — live dialog untouched; `lib/sosInviteQueue.js` `slotsFree = MAX − countActive − pending-without-session`; `lib/sipCryptoIdentifiers.js` `createSipTag` = 8 random bytes hex (was 0..9999) | Verified on read, no change: `isInviteInFlight` for `sosServerPull` already enforced at `startMediaDashboard` L15113 for all modes; replacement INVITE already BYEs the old dialog first (L819). WVP mode returns before the lock check (L15009 < L15134) so the lock never blocks an operator start there. `sipServer.js` / `pttServer.js` untouched (tag is a shared helper; hex token is standard SIP). | Lab test: 20 000 tags unique, hex16; lock acquire/block/other-cam/release; queue burst of 10 pending cams admits 8, queues 2. Note: the lock test wrote a few `sos connect lock` lines to `storage/fleet.log` (cams C1/C2, 23:15) — ignore. | Operator: cold SOS ×2 cams → one INVITE per cam, both live; ack → clear; PTT + DeviceControl still work (tag change). |

Checkpoint + `lab-git-push-sos` after Phase 1 PASS.

---

## Phase 2 — Air-gap ship blockers

| # | MOB | Files / lines | Change | Guard | PASS |
|---|---|---|---|---|---|
| 6 | `AIRGAP-MAP-OFFLINE-DEFAULT-V1` — **APPLIED 2026-09-02** | `public/js/map-offline-tiles.js` (rule: local pack exists → local; else `<meta name="fm-map-online" content="1">` → OSM; else blank tile + "Offline map pack not installed"; `fm-map-offline-only` always wins; fetch failure → blank unless opt-in; new `blankLayer()` export); `vms-command-shell.js`, `live-popout-minimap.js`, `ftp-inbox-ui.js` now call `MobilityMapTiles.attachLeaflet` (were hard-coded OSM); OSM else-branches → blank in `index.html` map-init, `route-trace.js`, `tactical-shell.js`, `dashboard-boot.js`; `maplibre-primary.js` `pickStyle` offline-first + no auto-hop to OpenFreeMap on error unless opt-in; `live.html` now loads `map-offline-tiles.js` (popout minimap had no helper); cache bust `?v=20260902-airgap-map-offline-default-v1` on 6 scripts | Frontend only — license / HWID / `.lic` untouched. Lab has `data/gis/offline/tiles` (12 zoom dirs) so lab maps render local. Remaining public URLs in `public/**` are exactly two, both behind the opt-in gate. `maplibre-primary.js` and `dashboard-boot.js` are not loaded by any page (dead) — aligned anyway. Pin-sync functions in `index.html` untouched. | 8 JS syntax OK; `rg` shows no other OSM/OpenFreeMap URL. **Side finding (not bundled):** `/api/gis/offline/config` returns `offlineRoot: GIS_OFFLINE_ROOT` — an absolute server path to the client. Violates the path-disclosure rule → candidate `AIRGAP-GIS-CONFIG-NO-ABS-PATH-V1`. | Operator: hard refresh Ops map, Tactical, VMS GIS, FTP inbox map, live popout minimap → tiles render, no `openstreetmap.org` in Network tab. Offline PC: no hang. |
| 7 | `AIRGAP-GEOCODE-AND-FETCH-TIMEOUT-V1` — **APPLIED 2026-09-02** | `lib/mapGeocode.js` new `isEnabled()`: default **OFF**; on only with `FM_MAP_GEOCODE_ONLINE=1` (public Nominatim) or `FM_MAP_GEOCODE_URL` (LAN geocoder); `FM_MAP_GEOCODE_DISABLED` / `FM_MAP_OFFLINE_ONLY` still win. `server.js`: new `GET /api/gis/geocode/status` `{enabled}`; geocode error body now `error = errorKey` (was `err.message`). `public/js/mobility-map-gis.js`: probes status on bind → hides search box when off; also hides on a 403 `map.placeSearch.offline`; search fetch bounded 12 s. Bounded fetches via guarded `AbortSignal.timeout` (no new shared file — a missing script tag on any of the 8 pages that load `i18n.js` would have been worse): `login.js` 5 fetches 10–15 s, `session-bus.js` 2 × 15 s, `i18n.js` locale 15 s (already falls back), `map-offline-tiles.js` config 8 s. Cache bust `?v=20260902-airgap-geocode-fetch-timeout-v1` on `index.html` (i18n, session-bus, mobility-map-gis) and `login.html` (login, i18n) | Server already had an 8 s Nominatim timeout — kept. Existing `.catch` / `.finally` handlers restore busy state on every bounded fetch (verified per call). Lab: search box disappears until you set `FM_MAP_GEOCODE_ONLINE=1` in `.env` (lab has internet) — opt-in by design. | Syntax OK (server + 6 JS). Enable-matrix test 5/5; `search()` off → 403 `map.placeSearch.offline`. | Operator: Ops map has no search box (expected); set `FM_MAP_GEOCODE_ONLINE=1` + restart → box back and search works. Login still works. |
| 8 | `AIRGAP-SIDECAR-NO-DOWNLOAD-V1` — **APPLIED 2026-09-02** | `weapon-sidecar/app.py`: `WEIGHTS_URL` removed; `_ensure_weights` local-only, raises `weights_missing: …` (surfaces in `/health` via `_model_err`; process stays up so Fleet sees a readable reason, not connection-refused). `anpr-sidecar/plate_yolo.py`: `download_crop_plate_model` local-only, one readable log line, returns `None` → caller keeps ph_id weights (existing branch). `START-ANPR.bat`: runtime `pip install ultralytics` removed → readable stop pointing to `INSTALL.ps1` offline wheels. `START-WEAPON.bat`: weights pre-flight (3 known files) → readable stop < 2 s. `scripts/PACK-SHIP-DELIVERY.ps1`: offline-only map meta on `index/login/live.html` + GIS tile copy now for **every** SKU (was CN only); warns if GIS pack or ANPR/Weapon weights missing from the staged pack | ANPR fast/heavy paths and weapon class maps untouched — only missing-weights branches. Lab verified on disk before edit: weapon `models/` has all 3 checkpoints; ANPR `models/` has plate_yolo11n + keremberke. Deviation from plan: weapon sidecar does **not** exit — it stays up with `/health` error (better operator signal). | Python AST OK ×2, PS1 parse OK, `rg` shows no huggingface / urllib / requests.get / pip install in the 4 runtime files. **Side finding (not bundled):** `Copy-Tree` excludes `.venv`, so first `START-*.bat` on a customer PC still runs `INSTALL.ps1` → pip → internet. Candidate `AIRGAP-SIDECAR-VENV-SHIP-V1` (ship venv or offline wheel pack). | Operator: START-WEAPON + START-ANPR boot as before. Rename `weapon-sidecar\models\checkpoint_best_total.pth` → START-WEAPON stops with readable message in < 2 s; rename back. |
| 9 | `AIRGAP-OUTBOUND-LINKS-V1` — **APPLIED 2026-09-02** | `case-files-ui.js`: SOS facts show plain `lat, lon` (Google Maps anchor removed). `lib/sosIncidents.js`: exported SOS report HTML prints `Location: lat, lon` (no public link). `lib/dispatchShare.js`: `mapsNavigateUrl` → `geo:lat,lon?q=…` (officer's on-device map app, no host); dispatch text line is `Location: lat, lon`. `lib/oidcAuth.js`: discovery / token / userinfo fetches bounded `AbortSignal.timeout(10 s)`. `lib/officerNotify.js`: webhook target must be private/LAN host (10/, 172.16–31, 192.168, 127, localhost, `.local/.lan/.internal`, bare hostname, IPv6 ULA) unless `FM_NOTIFY_ALLOW_PUBLIC=1`; Twilio already off unless `FM_TWILIO_ENABLED=1` (verified). Cache: `case-files-ui.js?v=20260902-airgap-outbound-links-v1` | Lab `.env` has no notify / OIDC keys → zero behaviour change in lab. Allowlist unit-checked (7 hosts + opt-in) PASS; all 4 libs load. | Case Files → SOS facts shows coordinates, no link. Export SOS report → "Location:" line. |

---

## Phase 3 — Browser stability (Investigation / Conference / Evidence; cache bust + hard refresh)

| # | MOB | Files / lines | Change | Guard | PASS |
|---|---|---|---|---|---|
| 10 | `INV-TAB-ONHIDE-TEARDOWN-V1` — **APPLIED 2026-09-02** | Root cause: `evidence-manager.js` tab router never called `VmsInvestigationTimeline.onHide` → `tl.visible` stayed true forever (hidden Sync RAF + tile videos + document key/wheel capture). Fix: router calls `onHide` on every non-Investigation tab (+ `ConferenceHub.onHide`). `vms-investigation-ui.js`: new `tlTeardownForHide()` = stop master clock (RAF cancelled) → per tile capture playhead, `pause → AxiomFlvManager.detach → src='' → load()`; `onHide` idempotent (early return if already hidden). `tlAssignCameraToTile` stops master clock before reassign. `tlBindCamFields` video teardown reordered pause→detach→unload. Two unguarded Escape handlers now check `tl.visible`. `conference-hub.js`: `onHide` = `stopLobbyPresenceWatch` + hide ingress toast (does NOT leave room). `map-popout-sync.js`: popout hello interval cleared + channel closed on `pagehide`. Cache: `?v=20260902-inv-tab-onhide-teardown-v1` on `vms-investigation-ui.js` (index + vms-investigation.html), `evidence-manager.js`, `map-popout-sync.js`, `conference-hub.js` (via `vc-lazy.js`) | `video-wall.js` / Ops wall players untouched — only Investigation tile `<video>` elements. Playheads kept → Play All resumes. Active VC call survives tab switch. `node --check` PASS ×4. | Play Sync in Investigation → switch to Ops: audio stops, CPU drops; return → Play All resumes from kept marker. Arrow keys / wheel on Ops map no longer hijacked. |
| 11 | `INV-SYNC-SEEK-GENERATION-V1` — **APPLIED 2026-09-03** | `vms-investigation-ui.js` only. `tlSeekTile`: per-tile `_seekGen`; after every await (1.5 s load timer / `loadedmetadata` / `seeked`) re-checks newest-gen + same cam + same `activeSegmentId` + same `<video>`; stale → resolves `false`, never touches the element (old load timer used to seek a newer clip to an old offset). `_seekBusy` flag spans the async window. `tlSeekAll`: `_seekAllGen` supersession — an older Sync-Lock seekAll that resolves later does not play/redraw; per-tile play skipped when its seek was `false` or the loaded segment is no longer the one under yellow. Compare layout: `var tile` in for-loop closure fixed (every `.then()` saw the LAST tile → one slot played) via `compareJob(tile)`. `tlPlayTile` honours `false`. Master-clock join: `false` with `activeSegmentId` still set = superseded → leave newer load alone (real refusal still clears). Transport chrome: `pause` media event flips `tile.playing=false` + redraw when not `_seekBusy`, not master clock, `readyState>=2`. Cache: `?v=20260903-inv-sync-seek-generation-v1` (index + vms-investigation.html) | Master clock tick logic (`INV-SYNC-EOF-STOP-YELLOW-V1` PASS) untouched except the `ok===false` branch. `tile.playing=true` before seek kept (transport shows Pause during load as before). `node --check` PASS. | Fast scrub (5 clicks/s) then Play All: each slot shows its own cam at yellow. Time Compare: 4 tiles play. Pause button state matches real playback. |
| 12 | `VMS-PLAY-GESTURE-HYGIENE-V1` — **APPLIED 2026-09-03** | `shared-flv-player.js`: new `playMutedOrArmGesture` / `armGesturePlay` — attach() and visibility-resume fall back to muted play; if still refused the `<video>` gets `data-fm-needs-gesture="1"` (blue outline, pointer) and the next click on it plays. `vms-investigation-ui.js` wall cells + `vms-command-shell.js` rail: bare `video.play()` after `attach()` removed (attach already plays; it only produced unhandled `NotAllowedError`); kept only when no manager. `fr-alarm.js` snapshot dialog: play rejection → muted retry → native controls. `fr-offline-video.js`: play gated on `loadedmetadata` (skips if src changed), muted retry. `vms-playback-ui.js` `loadSegment`: `canplay` handler ignores a superseded segment; rejection → muted retry → controls + status "Click the video to play". `hq-alert-audio.js`: persistent banner `#hq-alert-audio-unlock` ("Alert sounds are waiting for your first click…") shown when `play()` / `startHold()` fire before `unlocked` or custom tone `Audio.play()` rejects; hidden by `unlock()` (existing document click capture). CSS appended to `css/global.css`. Cache: `?v=20260903-vms-play-gesture-hygiene-v1` on 7 scripts + `global.css` (index, vms-playback, vms-investigation, live, matrix, command-wall) | `tlPlayVideoElement` and `AxiomFlvManager.attach` signatures untouched. Live tiles are muted already → no audio-policy change. `node --check` PASS ×7. | Fresh console (no click) → SOS: banner at bottom "waiting for first click"; click anywhere → banner gone, tone plays. FR snapshot dialog and VMS Playback never sit on a frozen frame without a visible way to play. |
| 13 | `INV-LIVE-REFRESH-AND-CACHE-V1` — **APPLIED 2026-09-03** | `vms-investigation-ui.js`: `onSosAlarm` no longer drops alarms while the tab is hidden — cam goes into `_sosDirtyCams`, `onShow` calls `tlFlushSosDirty()` → lanes re-fetched, red pin appears without Load/reload; visible alarms coalesce per cam (600 ms, `tlQueueSosRefresh`) → one merge + one draw per burst. `tlApplyLayout`: slots hidden by the layout now `capture playhead → playing=false → tlUnloadTileVideo` (was pause only; FLV session + buffer stayed alive) — re-seek on show is existing `tlSeekAll` path. `evidence-hub.js`: new `EvidenceHub.invalidateCatalog()` drops the 60 s warm cache for overview/docks/catalog/redacted-exports and, if the Evidence view is on screen, refreshes the visible list once (1.5 s debounce). Trim workspace: `loadedmetadata` sets In/Out `max` + clamps Out to duration (catalog-opened workspace had NaN duration); throttled 4 Hz `timeupdate` puts the live playhead in the Set In / Set Out titles (no layout change). `evidence-manager.js`: binds `ftp-upload` + `evidence-sorted` on `__mobilityDashboardSocket` → `invalidateCatalog` (root cause: old handler used `global.socket`, which is never set — it never fired). `live-player-factory.js`: named `timeupdate` / `ended` / `playing` handlers removed in `cleanup()`, then detach → pause → unload → remove. Cache: `?v=20260903-inv-live-refresh-and-cache-v1` (index, vms-investigation, live, matrix, command-wall) | Redraw only while `tl.visible`; nothing touches `video-wall.js`. No new DOM / copy except button `title`. `node --check` PASS ×4. | Open Investigation with a cam loaded, switch to Ops, raise SOS on that cam, return → red pin already there. Evidence catalog open → dock upload lands → list refreshes within 2 s. |

Checkpoint + `lab-git-push-investigation` after Phase 3.

---

## Phase 4 — Alarm marker contract

| # | MOB | Files / lines | Change | Guard | PASS |
|---|---|---|---|---|---|
| 14 | `ALARM-MARKER-CONTRACT-V1` — **APPLIED 2026-09-04** | `lib/vmsAiAlarmIndex.js`: `toIsoUtc` gate in `upsertMarker` (non-ISO skipped + warned); `sosOccurredIso` ISO fields first (`at`, `alarmAt`, `startedAt`, `createdAt`) — locale `alarmTime` no longer parsed; `logLiveSosAlarm(entry, {mergeAt})` → repeat press gets its own pin id (`sos:<id>:<mergeAt>`, note `· repeat`), never moves the first pin; new `repairNonIsoMarkers()` (regex `!~ ISO` LIMIT 5000: parseable → rewritten UTC, else deleted, counts logged). `lib/deviceAlarm.js` merge path calls `logLiveSosAlarm(open, {mergeAt})`. `server.js`: `bootstrapSiteDatabase` runs repair before `operationOverlay.init`; timeline alarm query `SELECT … cam_id …` + `occurred_at::timestamptz` in WHERE/ORDER; recording-days VIP list = `('analytics','sos','anpr')` (dropped CHECK-illegal `weapon` / `fr_hit` / `anpr_hit`); alarmDays catch → `log.web.warn` (was `console.error`); `emitSosAlarmToDashboard` skips marker write on `refresh` / `replay` pushes. `lib/vmsAlarmEventPreview.js` `resolveSos`: alias set via `resolveAliases`, ISO fields only. `lib/vmsForensicExport.js` `loadAlarmsInRange`: alias set + `ANY($1::text[])` + casts, `SELECT cam_id`. `vms-investigation-ui.js`: single `tlAlarmAt(al)` (`occurred_at` / `occurredAt` / `at`) replaces 13 ad-hoc readers; `tlPinColor` yellow for `line_crossing`; `tlIsCalendarVipAlarm` = same 3 types; **root cause** timeline `?date=` fetch sent no `tzOffset` → server used UTC day while the calendar used browser day — now both send it; calendar fetch catch logs `console.warn`. Cache: `?v=20260904-alarm-marker-contract-v1` (index + vms-investigation.html) | REST shape stays snake_case. Locked files untouched (`video-wall.js`, `pttServer.js`, pin-sync). `ONVIF vmsAlarmLogger` already ISO. `node --check` PASS ×6; module load OK. | Non-UTC browser: calendar red dots and timeline pins agree on the day; press SOS twice on an open incident → two red pins (second `· repeat`); Export clip sidecar lists markers under GB id alias; server log shows `alarm marker repair` counts once if old rows existed. |
| 14b | `SOS-OPEN-INCIDENT-RECORD-CONTINUITY-V1` — **APPLIED 2026-09-04** | `lib/liveCapture.js` `startWvpFromFlv` exit handler + `onWvpChildDied` / `scheduleSosSegmentRestart`; `lib/sosIncidents.js` `attachServerRecording` primary branch; `lib/deviceAlarm.js` merge path; `lib/vmsAiAlarmIndex.js` `logLiveSosAlarm` | Root cause (log 19:23:50–19:24:38): BWC press 2 = SOS off → FLV ends → HQ ffmpeg dies silently, `active` still says recording until ack → presses 3–5 merged with no capture. Fix: unexpected ffmpeg exit while incident open → finalize file as segment N (skip empty), restart N+1 (backoff 2 s→15 s, cap 10 segments / 40 tries); intentional stop / ack / clear-pack cancels. Later segment appends `serverRecordingSegments[]` + `server-recording-segN.mp4` — never overwrites first clip. Merged fall on open SOS pins `Fall · repeat`. **Dropped by design:** re-send device `Record` on merge — conflicts with `SOS-RECORD-NO-TOGGLE-V1` (BWC Record toggles; device state unknown). | Segments visible in Investigation timeline now; SOS desk UI does not list `serverRecordingSegments` yet (UI MOB). | PASS: press SOS → press again (off) → press again → ack. Investigation: ≥2 blue clips, 3 red pins. Log: `ffmpeg ended unexpectedly` → `segment restarted`. |
| 14d | `WVP-FLV-UPSTREAM-EOF-12S-V1` — **APPLIED 2026-09-04, operator PASS (video plays, no flashing)** | `server.js` `pin-open` / `select-device` / `start-video` | Root cause: SOS opens pin → `startFastStatusPolling` 1 Hz DeviceStatus MESSAGE for 180 s → BWC closes RTP (FIN) + BYE every 2–20 s → ZLM unregisters → all FLV readers die. Guard `fastStatusPollAllowedForCam` (WVP handoff → skip) + `stopFastStatusPolling` on WVP start-video. Disc `MOB-DISC-WVP-FLV-UPSTREAM-EOF-12S-V1-20260904.md`. | Classic pool path unchanged. | PASS 2026-09-04. |
| 14e | `INV-TILE-HOLD-SCRUB-PLAY-EOF-GUARD-V1` — **APPLIED 2026-09-04** | `public/js/vms-investigation-ui.js` (`tlMasterApplyTiles`, `tlMasterClockTick`, `tlPauseTile`, `tlStopTile`, `tlPlayTile`, `tlSyncPlayVideos`, `tlSeekAll`, `tlOnTimelinePointer`, `tlOnTimelineClick`, new `tlReleaseTileHolds`, `tlResumeAfterScrub`, canvas `mouseup`); cache `?v=20260904-inv-tile-hold-scrub-play-v1` (index, vms-investigation) | (1) per-slot Pause/Stop set `tile.hold` — master clock skips held slots; all held → clock parks ("All slots paused"); tile Play / Play All / scrub release. (2) Release yellow on blue → auto-play (Sync: Play All; else focus; compare: focus tile); gap → stays paused; pin click never auto-plays. (3) media shorter than DB `end_at` → `tile._exhaustedSegId` → slot stays black for that segment, no reload→ended→reload flash; cleared on seek/scrub/segment change. | No DOM/copy/`video-wall.js`. Follow-up server: `liveCapture.registerCapture` `endAt` should be start + media duration (honest segment end) — `LC-SEGMENT-END-FROM-MEDIA-V1`. | PASS: Sync on 2 lanes → tile Pause holds only that slot, tile Play rejoins; drag yellow onto blue, release → plays; yellow in phantom tail of 19:23 clip → black, no flash. |
| 14f | `LC-SEGMENT-END-FROM-MEDIA-V1` — **APPLIED 2026-09-04** | `lib/liveCapture.js` `registerCapture` + new `probeMediaDurationSec` | Before encryption, read container `Duration:` via `ffmpeg -i` (8 s cap); `vms_recording_segments.end_at = startedAt + duration` (only if earlier than wall clock). Failure → wall clock as before. Log `live capture registered` now carries `mediaDurationSec`, `endAt`. | Existing rows keep phantom tails — 14e client guard covers them. | Test: SOS → ack → Investigation blue length == clip length; log shows `mediaDurationSec`. |
| 14g | `SOS-DESK-PTT-AUDIO-PLAY-V1` — **APPLIED 2026-09-04** | `lib/sosIncidents.js` `enrichEntry` (+`serverRecordingSegments[].previewUrl`); `public/index.html` inline `renderSosDualMedia` → new `renderSosExtraMedia` + third card `#sos-detail-extra-card` / `#sos-detail-extra-list`; `public/js/case-files-ui.js` gallery (`audio` tile kind; segments + PTT WAV; audio evidence by extension); `en.json` `sos.detail.extraMedia`, `caseFiles.sosFactPttAudio`; cache `case-files-ui.js?v=20260904-sos-desk-ptt-audio-play-v1` | PTT WAV plays in `<audio>` (never `<video>`), HQ clip 2..N and helper clips listed on the SOS detail dialog and in the Case File gallery. DOM built with createElement; preload none/metadata. Card hidden when nothing extra. | `dashboard-boot.js` not touched (not loaded). No CSS added. | Test: SOS + PTT talk → ack → SOS detail shows "PTT Audio and Extra Clips" with playable audio; Case File gallery shows audio tile. |
| 14c | `SOS-SDK-EVENT-MAP-V1` — **SHIP GATE** (after BWC SDK firmware, ~1 week) | `lib/deviceAlarm.js`, `server.js` alarm ingress | Map new SDK messages (SOS-off / repeat-press / record-state if any) onto the open-incident path; if SDK exposes device record state, revisit dropped `Record` re-assert. | Do not ship without re-running the 14b PASS on SDK firmware. | — |
| 15 | `ALARM-MARKER-SCHEMA-CLEANUP-V1` (Med, discuss first) | `023_vms_spatial_pins.sql`; `020` CHECK; note format; REST/socket mapper | Spatial columns populate-or-drop; `retain_marker` purge rule; structured `note` or remove dead bbox parser; one mapper for `wall-alarm` vs REST. | Product decision, not bug fix. | — |

---

## Phase 5 — DB and store races

| # | MOB | Files / lines | Change | Guard | PASS |
|---|---|---|---|---|---|
| 16 | `DB-UNIQUENESS-AND-LOCKS-V1` — **APPLIED 2026-09-04** | `db/migrations/032_evidence_relative_path_unique.sql` (new, in `siteDb.js` boot list); `lib/siteDb.js` `insertCaseExhibit` (`SELECT id FROM case_files … FOR UPDATE` before MAX), `updateCaseFile` (column-scoped SET from `patch` keys only, `undefined` skipped, `ops_case_id` fallback retries without that key); `lib/evidenceRegistry.js` `registerFromUpload` (on `23505` adopt winner row by `relative_path`, re-upsert) | Exhibit numbering serialized per case; case-file edits no longer clobber other fields; `UNIQUE(relative_path)` enforced. | Migration re-points `evidence_downloads/attachments/exports/secure_exports/case_file_evidence/evidence_meta/evidence_gps_traces` to the oldest survivor before delete. Ran on lab DB 2026-09-04: version 32 present, index present, 0 dupes left. | Two browsers add exhibits at once → #1 and #2, no 500. Edit title in one tab, narrative in another → both kept. |
| 17 | `JSON-STORE-SINGLE-WRITER-V1` — **APPLIED 2026-09-04 (complete; `lib/pttServer.js` included on user naming the file 23:24)** | new `lib/jsonStoreWriter.js` (`atomicWriteJsonSync`, `createStore` = cache + unique temp/rename); `lib/wvpSipLanMap.js` `readMap/writeMap` → store (fresh `updatedAt`); `lib/dispatchGroups.js` `readFile/writeFile` → store; `lib/conferenceStore.js` `readStore/writeStore` → store; `lib/pttEvidenceRecorder.js` per-process `fileSeq` suffix in WAV name + per-key `finalizeChains` (serial finalize); `lib/scalePrep.js` `FtpUsedShield.claim()`; `server.js` `ingestLatestFtpSnapshot` uses `claim()` (returns null if lost); `lib/pttServer.js` `flushLearnedCmdsSoon` write → `atomicWriteJsonSync` (same 400 ms debounce, still sync, no SIP/PTT audio path touched) | Torn file / torn read can no longer wipe a store on the next write; end()+begin() same ms cannot truncate the file being finalized; snapshot claimed atomically; learned-cmds file cannot be left half-written. Smoke: torn disk file → cache survives, fresh read → fallback, 0 `.tmp` left, claim true/false. | `sosIncidents.js` keeps its own MOB 3 writer (PASS, untouched). | 8 BWC re-register burst → LAN map keeps all peers; restart → dispatch groups / conference rooms intact; PTT login still seeds cmd 130. |
| 20 | `SOS-DESK-TIMELINE-SYNC-V1` — **APPLIED 2026-09-05** | Modal: `#sos-detail-backdrop` flex-start + 36px top pad, panel `max-height: calc(100vh - 64px)` in `global.css`. Jargon: `Owning SA` → `Case Owner` (en.json + case-files fallbacks + new exhibit title); missing `caseFiles.sosFact*` keys added (was showing raw "Sos Fact Hq Rec"). Extra card: WAV slaves (no native controls when HQ video exists) driven by `nowUtc = serverRecordingStartedAt + video.currentTime` on `timeupdate`/`seeked`/`play`/`pause`; orange ticks on `#sos-detail-talk-strip`; extra HQ/helper clips keep controls. Recorder now stores `startedAt` + `kind` on `attachPttAudio`. Cache `?v=20260905-sos-desk-timeline-sync-v1`. | Hidden audio if no HQ video keeps native controls so the file stays playable. Old WAVs without `startedAt` estimate start as `at − duration` after metadata. | — | Restart + hard refresh. Open an SOS with HQ clip + radio WAV: header not clipped; extra card lists bursts; play/scrub HQ video → radio follows; orange ticks on the strip. |
| 19 | `SOS-AUDIO-RECORD-GATE-AND-CALL-V1` — **APPLIED 2026-09-04 23:5x (architect-approved after 3-point challenge)** | `lib/pttFieldGroupRelay.js` `setOpenSosResolver()` + `isCamInOpenSos()` folded into `isPttEvidenceCam` (solo SOS = evidence cam; Ack closes gate by itself); `server.js` wires resolver after `pttEvidenceRecorder.init`, `emitBwcCallState` begin/end HQ call recording on inactive→active / active→inactive (single choke point: HQ hang-up, BWC BYE, INVITE fail, socket loss), `call-audio` → `appendHqCall`, `sosGroupCall.onState` begin/end group recording on idle→active / active→idle (SOS kind only), `sos-group-call-audio` → `appendHqGroupCall`; `lib/pttEvidenceRecorder.js` call sessions: `CALL-hq~cam~incident~startedMs~pid~seq.wav` written as `.part` then renamed, 5 min segment roll via indexed keys `call:<cam>:segN` (distinct session + finalize chain per segment), zero-fill only for gaps > 1000 ms (20 ms frames), 1 MB stream buffer, bind to incident captured at begin (survives Ack mid-call), boot sweep promotes orphan `.part` from the name and binds. PTT path byte-for-byte unchanged. | Officer voice is already in the HQ MP4 track (operator confirmed); WAV = HQ side only. Smoke: 2 s talk + 3 s gap → 4.98 s file bound to INC; 366 s paced → 2 segments, both bound; orphan `.part` → promoted, RIFF size correct, bound; PTT burst still `PTT-hq-*`. No SIP / RTP / 29201 / FLV code touched. | Next: `SOS-DESK-TIMELINE-SYNC-V1` (absolute-UTC master clock, hidden WAV slaves under primary video in SOS detail). | Solo SOS → Call → Ack → SOS desk third card lists `CALL-hq~…wav`, plays; PTT team → PTT still `PTT-hq-…wav`. |
| 18 | `PTT-AUDIO-FOCUS-AND-TEAMS-V1` (Med, discuss first) | `lib/liveStreamPool.js` 39, 144-217; `lib/pttFieldGroupRelay.js` 139-249; `lib/sipGroupCall.js` 258-296; `server.js` 18131-18177 | Per-viewer audio focus or multiplexed frames; per-target TX lock across SOS teams; group-call mix policy; alarm NOTIFY camId from SIP peer. | Group-call mixing is by design — product decision first. | — |

---

## Risk-assessment adjustments (2026-09-02, agreed before Phase 1)

1. MOB 3 is write-behind, not async-mutate — callers identical (done).
2. MOB 4 keeps `readActiveAlarm()` shape; adds per-cam getter beside it.
3. MOB 6 gated: lab keeps explicit online opt-in (meta/env) so lab maps never go blank; ship pack omits it.
4. MOB 11 splits: 11a compare-layout closure (Green) → 11b seek generation guards (Red).
5. MOB 14 and 16: read-only pre-flight count (non-ISO rows / duplicate `relative_path`) shown to user before any cast or constraint.
6. Phase 1 order: **3 → 1 → 2 → 4 → 5** (store single-writer first).

Grades: Green 9, 13, 17, 11a · Amber 2, 5, 7, 8, 10, 12 · Red 1, 3(done), 4, 6, 11b, 14, 16 · Discuss 15, 18.

## Rules of engagement (every MOB)

1. User types exact `MOB-APPLY <name>`; agent patches only listed lines; no bundling.
2. Server MOB → `RESTART-FLEET.bat`. UI MOB → agent bumps `?v=` in `index.html`, user hard-refreshes once.
3. PASS → disc line recorded → checkpoint. Genre push only on `lab-git-push-<genre>`.
4. FAIL on Phase 1 or 3 with pin/wall regression → user types `RUN RESTORE-ME8-FIRMWARE-SEP-GOLD`; otherwise agent reverts that single MOB.
5. Locked files touched only where the table says "user names file".

**Progress:** Phase 1 complete — MOB 3 PASS (operator 2026-09-02); MOB 1, 2, 4, 5 applied. Phase 2 complete — MOB 6, 7, 8, 9 applied. Phase 3 complete — MOB 10, 11, 12, 13 applied. Phase 4 — MOB 14 applied 2026-09-04 (restart + hard refresh needed; operator PASS pending). MOB 15 is a product decision (discuss first). (Tests pending, see table.) **Next:** Phase 5 first row after MOB 14 PASS.

## Pending operator tests (operator chose: MOB first, test all at the end)

| MOB | Test | Result |
|---|---|---|
| `PTT-HQ-SINGLE-FLOOR-V1` | Two HQ tabs, same BWC, PTT on both → first talks, second gets busy alert; release first → second talks. Single-tab PTT + SOS PTT team unchanged. | pending |
| `PTT-PER-SOCKET-VOICE-TARGET-V1` | A calls BWC; B tab call-mic → BWC does not hear B, log `call audio dropped — not call owner`; A reload mid-call → A can still end call. Single-operator call unchanged. | pending |
| `SOS-RAISE-ATOMIC-AND-PER-CAM-ACTIVE-V1` | SOS on 2 cams → both banners; ack cam 1 → cam 2 stays in alarm; ack cam 2 → all clear. Single-cam SOS → ack → +/− helper unchanged. | pending |
| `SOS-INVITE-GUARDS-V1` | Cold SOS ×2 cams → one INVITE per cam in log, both live. Snapshot / Record / PTT still work after restart (From-tag format changed). Log shows `sos connect lock acquired` once per cam. | pending |
| `AIRGAP-MAP-OFFLINE-DEFAULT-V1` | Hard refresh: Ops map, Tactical, VMS GIS, FTP inbox map, live popout minimap all show tiles; browser Network tab has no `openstreetmap.org` / `openfreemap.org`. Pins / video unchanged. | pending |
| `AIRGAP-GEOCODE-AND-FETCH-TIMEOUT-V1` | Login works. Ops map: no place-search box (default off). Add `FM_MAP_GEOCODE_ONLINE=1` to `.env`, restart → box back, search "Manila" works. | pending |
| `AIRGAP-SIDECAR-NO-DOWNLOAD-V1` | START-WEAPON + START-ANPR boot as before; weapon `/health` ok. Rename `weapon-sidecar\models\checkpoint_best_total.pth` → START-WEAPON stops with readable message < 2 s; rename back. | pending |
| `AIRGAP-OUTBOUND-LINKS-V1` | Hard refresh → Case Files → open an SOS-linked case: facts show `lat, lon` text, no "Open map" link. Export SOS report HTML → `Location:` line, no `google.com`. Dispatch share text shows `Location: lat, lon`. | pending |
| `INV-TAB-ONHIDE-TEARDOWN-V1` | Investigation: assign 2 cams, Play All (Sync) → switch to Ops tab: audio stops within 1 s, Task Manager CPU drops; arrow keys / mouse wheel on Ops map behave normally. Back to Investigation → yellow marker kept, Play All resumes. Conference: join a room → switch tab → return: still in room. | pending |
| `INV-SYNC-SEEK-GENERATION-V1` | Investigation, 2 cams, Sync Lock on: click the timeline fast 5× in a row then Play All → each slot shows its own cam at the yellow marker (no wrong clip / wrong offset). Time Compare layout: Play All → all 4 tiles play. Single tile Playback → Pause button shows only while video actually runs. | pending |
| `VMS-PLAY-GESTURE-HYGIENE-V1` | Hard refresh, do NOT click anything, trigger SOS → bottom banner "Alert sounds are waiting for your first click"; click anywhere → banner gone, tone sounds. Ops wall / VMS rail / Investigation wall still auto-play (muted) as before; console has no `Uncaught (in promise) NotAllowedError`. FR snapshot dialog plays on open. | pending |
| `INV-LIVE-REFRESH-AND-CACHE-V1` | Investigation: load today for a cam → switch to Ops → raise SOS on that cam → back to Investigation: red pin present without Load. Evidence → Catalog open → dock/FTP upload lands → list refreshes by itself within 2 s. Layout Single: switch focus slot → hidden slot releases (no audio), focus slot re-seeks to yellow. | pending |

**Side MOB 2026-09-03 `MAP-OPS-LOCAL-ZOOM-AND-FILL-V1` (applied):** `lib/gisOffline.js` reports `maxNativeZoom` from `tiles/{z}` folders (number only). Leaflet uses that as native max + 1 overzoom (last tile stretches, no empty z19). After attach: `invalidateSize` now + 80 ms. Country presets (SG/PH/ID/TH/KR/CN/ZA) clamp fly/search zoom to the same pack ceiling. `.leaflet-container` 100% width/height in Ops wrap. Cache `?v=20260903-ops-local-zoom-fill-v1`. Restart + hard refresh. Zoom in until the control stops — streets stay painted. Switch country dropdown — same fill, no blank zoom.

**Side MOB 2026-09-03 `MAP-STACK-HUD-BELOW-GEO-TOOLS-V1` (applied, CSS only, `index.html` `#map-pin-stack-hud`):** stack HUD moved from top-centre (was covering Geo Tools / Fit pins / place search) to second row, left-anchored beside the zoom control (`top: 54px; left: 52px`), width capped so the place-search results dropdown on the right stays clear. Test: open 2 co-located pins → HUD sits under the toolbar, all geo tools clickable.

**Lab note 2026-09-02 (+helper first click slow, not a regression):** fleet.log 22:42 — helper cam got `Record` 0.8 s before the WVP INVITE; WVP `startPlay` hit `wvp_stream_timeout` (10 s), handoff auto-retry recovered at +14 s. Candidate later MOB (parked, not bundled): `SOS-HELPER-RECORD-AFTER-LIVE-V1` — send `Record` after the FLV proxy opens.
