# MOB DISC — Panel video box fixed 300×136 · not pin size logic

**No code. Confirm only.**

---

## Got it?

**Yes.**

| Your rule | Locked |
|-----------|--------|
| Panel **video box only** | Fixed **300×136** (basic pin size) |
| Pin’s size-change rules | **Do not follow** (compact / crowded / 96 / 80 / density ladder — **ignore for panel**) |
| Panel stays | Always **300×136** for the video stage — no shrinking when many pins open |
| Chrome | Head above / telemetry below — outside the 300×136 box |
| Wall | May widen / shift left a bit to fit 300 wide |
| Pin itself | **Do not change** pin size logic |

---

## Why this matters

Pin is allowed to get smaller when the map is crowded.  
Panel must **not** copy that. Panel = one stable ops tile: **300×136** always.

---

## APPLY name (when you want code)

`MOB-APPLY-PANEL-VIDEO-BOX-MATCH-PIN-SIZE`

Means: fixed **300×136** stage on wall panels — **not** wired to pin compact/crowded classes.

---

## One line

> **Got it. Panel video box = fixed 300×136. Never follow pin size-change logic.**
