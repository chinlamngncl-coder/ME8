# MOB DISC — Why InsightFace pack was applied · take it off · Seeta is the path

**Status:** Operator directed **take pack off** → APPLY `mob-fr-onnx-pack-off`  
**Date:** 2026-07-14  

## APPLIED `mob-fr-onnx-pack-off`

- Lab pack back to **small** (`buffalo_sc`) — heavy pack **removed**  
- **Not** switched to DeepFace  
- Next quality path remains **Seeta**

```text
MOB-APPLY mob-fr-seeta-windows-lab-proof   ← APPLIED (lab proof on :8767)
MOB-APPLY mob-fr-rail-blur-reject
```

---

## Straight apology

You asked for a **stronger face engine** after match-debug failed under 70%.  
I applied **`mob-fr-onnx-pack-upgrade`** (InsightFace bigger pack).

That was **the wrong fork** once the direction was **Seeta (Chinese, legal-free story)**.

**That was my mistake. Not yours.**

---

## Settled path (locked)

1. **SeetaFace6 open** = next real engine work  
2. **InsightFace pack upgrades** = **off**  
3. **DeepFace** = not the recovery plan  
**Tone:** Operator is right to be angry. Plain English.  

---

## “Take it off” — what that means

Tonight’s change put the lab on the **bigger InsightFace pack** (and slower grabs partly because of that).

**Take it off** = undo that pack setting so lab is not stuck on the heavy pack while we build Seeta.

| Action | MOB (you type when ready) |
|--------|---------------------------|
| **Undo InsightFace pack upgrade** | `MOB-APPLY mob-fr-onnx-pack-off` |
| **Then** Seeta first proof | `MOB-APPLY mob-fr-seeta-windows-lab-proof` |

`mob-fr-onnx-pack-off` will:

- Put pack back to the **small** lab pack (or clear the forced big pack) so CPU/snap load drops  
- **Not** switch you to DeepFace  
- **Not** claim match will suddenly clear 70%  
- Leave Seeta as the **next** quality bet  

Snap speed MOB (`mob-fr-snap-grab-faster`) can **stay** — that helps Recent either way.

---

## What we will **not** do next

- Another InsightFace “try bigger pack” MOB  
- DeepFace as the main fix  
- Lower the **70%** bar  
- Pretend tonight’s pack was progress  

---

## Bottom line

**Why did I do it?** Wrong priority after a match fail — chased InsightFace instead of Seeta.  
**What now?** You already settled **Seeta**. Take the pack off, then Seeta lab proof.

```text
MOB-APPLY mob-fr-onnx-pack-off
```

After that:

```text
MOB-APPLY mob-fr-seeta-windows-lab-proof
```

No code until you type APPLY.
