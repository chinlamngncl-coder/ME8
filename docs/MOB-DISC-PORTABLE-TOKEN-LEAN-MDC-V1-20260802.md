# MOB APPLIED — PORTABLE-TOKEN-LEAN-MDC-V1 (2026-08-02)

## Applied

Created portable template:

`.cursor/rules/token-lean.mdc` (`alwaysApply: true`)

## How to reuse on other projects

1. Copy `token-lean.mdc` into that repo’s `.cursor/rules/`.
2. Better long-term: paste the same 7 lines into **Cursor → Settings → Rules → User Rules** once.

## Note

ME8 also has `me8-token-lean-patches.mdc` (same idea + ME8 APPLY reminder). Keeping both is fine; User Rules still win for all projects.
