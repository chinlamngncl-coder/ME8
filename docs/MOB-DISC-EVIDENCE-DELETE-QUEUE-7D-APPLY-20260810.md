# MOB DISC — EVIDENCE-DELETE-QUEUE-7D-V1 APPLY (2026-08-10)

**Status:** APPLIED. Operator PASS pending.

## What

1. Library detail **Delete (7-day queue)** — soft-queues; file leaves Active Library; disk kept.  
2. Evidence → **Deletion queue** (Super admin) — list, **Restore**, **Purge due now**.  
3. Auto purge tick with GPS retention interval (~6h).  
4. **Not** the same as Archive (cold storage).

## Check

Restart → hard refresh → open Library file → Delete (7-day queue) → appears under Deletion queue → Restore → back in Library.

## Next

`REDACT-LICENSE-GREY-BUTTON-V1` or dock dual-record when dock ready.
