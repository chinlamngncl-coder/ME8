# MOB-DISC — Evidence: Media Pool label + Docking Operations (monitor stays)

**Date:** 2026-08-17  
**APPLY:** `MOB-APPLY EVIDENCE-MEDIA-POOL-AND-DOCKING-MONITOR`  
**Scope:** Evidence hub labels + Media Pool copy. Keep existing `#ev-dock-bay-grid`. No Settings. No new APIs. No SHA-256 invention. No Route 650px.

## What this APPLY is

No prior disc existed. This is **not** the refused 7-module packet.

- Tab `evidenceHub.navDocks` → **Docking Operations** (panel `#ev-panel-docking` unchanged). Bay grid already is the hardware monitor.
- Tab `evidenceHub.navUnassignedEvidence` → **Media Pool** (panel `#ev-panel-ftp-inbox` unchanged). Super-admin gate **stays**.
- Empty copy + bulk confirm: “unassigned” → Media Pool. **Bulk Purge** on this toolbar → **Bulk Clear** (id `ftp-inbox-bulk-purge` stays).

## What this APPLY is not

- Do **not** remove `#ev-dock-add` / `#ev-dock-add-toolbar`. Settings `#ss-panel-docks` has no register form — deleting Evidence register would strand docks.
- Do **not** invent SHA-256 NEW/DUPLICATE badges, Source/Officer filters, or a new bay SVG set.
- Do **not** jump to Settings. Do **not** touch `#ss-panel-docks` shortcut.

Register-on-Settings is a **later** named APPLY.
