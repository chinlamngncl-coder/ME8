# MOB DISC — Same picture on panel and pin · what to do (suggest only)

**No code. No APPLY. Suggest only.**  
**Rule:** do not destroy / touch other things until you name one APPLY.

---

## Goal (your words)

Panel wall and pin must show the **same** video framing.  
We **cannot** keep two different pictures of the same live.

---

## Why they differ today (check)

| | Panel wall | Pin |
|--|------------|-----|
| Fit now | Fill box (`cover`) — no side black | Full scene (mirror / full frame) |
| Result | Cuts a bit **top + bottom** | Shows **more** |

Same stream. Panel crops in CSS. Pin shows the whole frame. That is the mismatch.

---

## What to do — honest paths

You need **one** shared rule for both.

### Path A — Both show the **full** frame (recommended)

**Meaning:** Panel looks like pin (flowers + timer / full scene).  

**How (when you APPLY later):**  
- Panel back to **full-frame fit** (like pin), **and**  
- Make the **panel video box taller** (closer to camera / pin shape) so full-frame does **not** bring back thick black sides.  
- **Pin: do not touch.**  
- Bank B size (same as 1–5, blank below): **do not touch.**  
- Call / PTT / SOS: **do not touch.**

**Risk if we only flip fit and skip taller box:** black sides return on short panels.

### Path B — Both **crop** the same (not recommended)

Make pin crop like the panel (`cover` everywhere).  
Same look, but **both** lose top/bottom. You already disliked panel vs pin when panel crops.

### Path C — Leave mismatch

Not allowed by your goal (“cannot sacrifice showing 2 things differently”).

---

## Suggestion (one line)

**Do Path A when you are ready:**  
*panel = full frame like pin + taller panel stage so sides stay gone; pin and banks and voice left alone.*

---

## Suggested APPLY name (only if you want that)

`MOB-APPLY-PANEL-MATCH-PIN-FULL-FRAME`

Scope locked in that name:

1. Ops `#video-wall` panel stage / video fit only  
2. Full frame like pin  
3. Adjust panel stage height/AR only as needed so sides do not return  
4. **No** pin file freestyle · **no** bank B size change · **no** Call/PTT/SOS  

If Path A needs a tiny follow-up MOB, say so after you test — not bundled.

---

## What I will not do until you say APPLY

- No edit  
- No cover/contain flip on my own  
- No “fix pin to match bad panel”  
- No other features

---

## One line

> **Suggest: make panel full-frame like pin (taller panel box if needed). Pin stays. You name APPLY when ready.**
