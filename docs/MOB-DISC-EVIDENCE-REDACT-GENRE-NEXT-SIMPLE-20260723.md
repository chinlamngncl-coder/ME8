# MOB DISC — Evidence redact genre: where we are · what’s next (simple English)

**Date:** 2026-07-23  
**Status:** LIVE CHECK **PASS** 2026-07-23 — one Finalized redacted copy shows; Second pass path usable. Evidence redact UX pile for this arc can pause.  
**Optional later:** genre git push only when you ask (`MOB-APPLY lab-git-push-…`).  
**Audience:** Operator (not tech)

---

## What we already fixed (this arc) — PASS where you said so

| Plain English | MOB name |
|---------------|----------|
| Destroy Finalized redacts needs **your password**, not typing DELETE | `REDACT-CLEAR-FINALIZED-PASSWORD-CONFIRM-V1` |
| Blur-duration dropdown wording (Package F) | `REDACT-SPAN-LABEL-COPY-V1` |
| Stop endless Finalize loop when already done | `REDACT-FINALIZE-DONE-NO-LOOP-V1` |
| Super admin Prior actions show for **redacted** rows | `PRIOR-FINALIZED-SUPERADMIN-ACTIONS-V1` |
| Trim copies labeled **[Trim]** + **Remove** / Clear (password) | `PRIOR-TRIM-LABEL-AND-REMOVE-V1` |
| Prior list not split into a tall ugly tower | `PRIOR-EXPORTS-ROW-COMPACT-UNIFY-V1` (**PASS**) |
| Redacted exports empty page no blank scroll | `REDACTED-EXPORTS-EMPTY-SCROLL-COMPACT-V1` (**PASS**) |
| Empty Redacted exports explains trims vs redacts + how to create one | `REDACTED-EXPORTS-EMPTY-HINT-V1` (**PASS**) |

**Lab fact (checked earlier):** catalog had **0 redacted** files and **2 trims**. That is why Redacted exports was empty and why Second pass was missing — not because Super admin was blocked.

---

## What “Second pass” still needs (no new MOB yet)

Second pass only appears on a **`[Redacted]` Finalized** row.

You do **not** get it from trim **Download** alone.

---

## Recommended next (one path) — live check, not more inventing

**No `MOB-APPLY` yet.** You run this once:

1. Evidence Library → open a **source** recording.  
2. **Redact** → draw blur → **Save**.  
3. **Finalize & register** (password / note as usual).  
4. Check:
   - Prior exports shows **`[Redacted]`** with Download · **Second pass** · Remove  
   - Redacted exports tab shows **at least 1** (All time · Finalized)

| Result | Then |
|--------|------|
| **PASS** | Evidence redact UX for this pile is done enough to pause; Second pass is usable |
| **FAIL** (Save/Finalize but still 0 on the tab) | Say FAIL — next is a **named MOB** to fix burn/register (we write that disc then) |

**Why this next, not another wording MOB:** the empty hint is already PASS. More UI text will not create a redacted file. One real Finalize proves the path.

---

## After that PASS — optional later (only if you ask)

| Plain English | Possible later APPLY |
|---------------|----------------------|
| Genre git push when you want history saved | `MOB-APPLY lab-git-push-evidence-redact` (you name it) |
| Filter row on Redacted exports tighter chips | only if it still bothers you |

Do **not** stack those until the live Finalize check is done.

---

## One line

**Next = you Finalize one real redacted copy; if it shows on Prior + Redacted exports, this genre’s main path is done — say PASS or FAIL.**
