# MOB DISC — “BWC black + can’t off” — from **your conversation**, not the log

**Status:** DISC corrected 2026-07-10 — prior disc over-weighted `fleet.log`; this one follows **your words** in chat.  
**Search:** `black screen`, `can't off`, `take it off`, `2nd fix`, `destroyMapPlayer`, `fallback`  
**Transcript:** [Chin black live invite stuck](a055f949-b6c0-443a-82f9-0fcf322b513b) (same long thread)

---

## Your exact words (the keyword)

**2026-07-05 22:27 — you:**

> worst. total failure. Mob disc. **bwc on black screen now and can't off.** the previous agent did the same shit. please **do not go there anymore**, you **2nd fix is bad**. **take it off.**

That is what **dead / black / can’t off** means in this project — **your** language for that failure.

**Agent reply then (locked lesson):**

> The 2nd MOB’s **`destroyMapPlayer` on reattach** was killing players mid-stream → **black wall, stop button dead**. That path is out.  
> **I won’t touch pin sync with destroy/reattach storms again.**

---

## What “you fallback?” meant in your mouth

From **day one of this thread** you said agents must respect:

> strict rules, in **apply**, in **fallback**, in all the functions that are done could not be touch

So **fallback** ≠ “read invite_stuck in the log.”  
**Fallback** = restore / baseline / bring-back of live code — and **not** re-entering the patch path that already burned you.

Tonight you also said the problem you **had and solved** is **back** — same **black + can’t off** feel as after the bad 2nd pin MOB.

---

## What the conversation says caused it (then)

| Step | MOB | Result |
|------|-----|--------|
| 1 | `mob-me8-wall-decode-sync-pin` | Thin; checkpoint still FAIL (pin Live streaming) |
| 2 | `mob-me8-pin-reattach-stable` — includes **destroyMapPlayer** reattach | **YOUR FAIL:** black screen + **can’t off** |
| Fix | **Take it off** — revert `video-wall.js` | You ordered; agent reverted |

**Locked forever from that chat:** do **not** re-apply destroy/reattach pin storms. Same shit as previous agent.

Later path in the same thread moved to canvas mirror / POC restore — **not** “patch destroy again.”

---

## Tonight vs that conversation

| Tonight (your report) | Jul 5 conversation |
|-----------------------|--------------------|
| Chin black pin + Panel 1, Live streaming… | **bwc on black screen** |
| Stop live feels dead / can’t off | **can't off** |
| “same problem… solved… now back” | 2nd fix bad → take it off → later live recovered via other path |
| “you fallback?” | Don’t break apply/fallback rules; don’t re-enter bad live patches |

**Correction to the earlier disc:** leading with `invite_in_flight` / `pool no rtp` was **log-first**. You asked to **check the conversation**. The conversation’s smoking gun is **`destroyMapPlayer` reattach → black + can’t off → TAKE IT OFF — never again.**

---

## What agents must not do (from your chat)

1. Do **not** re-introduce pin **destroy/reattach** “fixes” in `video-wall.js`.  
2. Do **not** treat “fallback” as an excuse to thrash locked live files.  
3. Do **not** ignore your phrase **black screen + can't off** as a soft UI glitch — it is the **hard fail** you already named.

---

## Bottom line (conversation-only)

You already defined the failure: **BWC black screen + can’t off** after a bad live/pin MOB.  
You already ordered: **take it off / do not go there anymore.**  
Tonight’s anger = that **same symptom class is back**, and you asked whether **fallback** put you there again — not for another log essay.

Next step only when you say so (e.g. restart, or name a restore MOB). No APPLY from this disc.
