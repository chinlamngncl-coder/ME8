# MOB DISC — CREDIT-LEAN-HARD (invoke code) (2026-08-09)

**Status:** LOCKED. Operator has zero trust in soft “I won’t scan” promises.  
**Also:** `.cursor/rules/me8-credit-lean-hard.mdc`

---

## Your code (paste any time)

```text
CREDIT-LEAN-HARD
```

Aliases (same meaning):

```text
MOB-LEAN-NO-SCAN
NO-SCAN-THIS-TURN
```

When you paste **any** of those in a message, the agent **must** obey Section A for the rest of that turn (and the next APPLY in that turn). No debate.

---

## Section A — Hard limits (credit burn)

| Allowed | Forbidden |
|---------|-----------|
| Open **1–3 files** the APPLY / question already named | Repo-wide Grep/Glob “to be sure” |
| Patch the exact strings/blocks needed | Parallel explore / Task “search codebase” |
| One short verify if smoke needs it | Reading whole `server.js` / giant HTML for a copy fix |
| Short reply | Long diagnosis tours, option menus |

**Cap:** Prefer **zero** search tools. If a path is unknown, ask **one** question for the path — do not hunt.

**Violating this after the code is pasted = agent failure** (same seriousness as APPLY without go-ahead).

---

## Standing default (even without the code)

Still obey `token-lean` / `me8-token-lean-patches` / `MOB-DISC-AGENT-CREDIT-BURN-NO-SCAN-20260809.md`.  
The code above is the **hard override** when you do not trust the agent.

---

## What you do

Paste `CREDIT-LEAN-HARD` at the start of a message or next to `MOB-APPLY …`.  
Example:

```text
CREDIT-LEAN-HARD
MOB-APPLY OPS-CASE-UI-COPY-PLAIN-V1
```
