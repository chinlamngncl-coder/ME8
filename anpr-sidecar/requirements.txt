# MOB-601 ANPR sidecar — lab / ship pack like FR
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
numpy>=1.24,<3
opencv-python-headless>=4.8,<5
Pillow>=10,<12
# OCR — RapidOCR ONNX (Paddle models without paddlepaddle/torch DLL clash)
rapidocr-onnxruntime>=1.3,<2
# hatch legacy (do not import in live dual_lpr path)
# paddlepaddle / paddleocr removed from ship default — use RapidOCR
hyperlpr3>=0.1.3,<0.2
# Plate detect (ANPR-PLATE-YOLO-PACK) — ONNX, no torch
onnxruntime>=1.16,<2
# Stage-2 PH/ID plates YOLO (ph_id_plates_best.pt) — required, no CCPD swallow
ultralytics>=8.1,<9
# ANPR-FASTALPR-EVAL-V1 — free global ALPR bake-off
fast-alpr[onnx]>=0.3
# Windows: Paddle needs protobuf<=3.20.2; re-pin after onnx/fast-alpr
protobuf>=3.19.0,<=3.20.2; platform_system == "Windows"
