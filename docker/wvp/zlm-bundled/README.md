# WVP bundled ZLM config (Track B lab)

**Source:** copied from running `me8-wvp` → `/opt/media/config.ini`  
**MOB:** `mob-wvp-bundled-zlm-persist`

Mounted read-only into the container by `docker-compose.wvp.yml`.

**Do not confuse with** `docker/zlm-config/config.ini` (Fleet `me8-zlm`).

Stamp / timeout tunes for Tile soak edit **this** file, then recreate `me8-wvp`.
